# AI 项目库 AIPM 入口

状态：ACTIVE

AIPM 是长期 AI 项目的**总控与路由层**，不是所有项目细节的集中副本。

## 先判断：需要 AIPM 吗？

### Fast Path｜项目已经明确
如果用户已经明确说出具体项目/产品，且执行仓库已知：
- 直接进入对应执行仓库的 `AGENTS.md` / START_HERE / Skill；
- 按该入口读取最小必要上下文；
- 不为了流程完整而强制扫描 AIPM。

### Discovery Path｜项目不明确或跨项目
如果任务模糊、可能跨多个项目、需要判断入口/优先级/依赖：
1. 读 `PROJECT_INDEX.md`；
2. 找到对应 Project ID；
3. 读该项目 `CURRENT_STATE.md`；
4. 按索引跳转到执行仓库硬入口；
5. 再由执行仓库决定需要读哪些事实、规则和资产。

完整架构见：`ARCHITECTURE.md`。

## AIPM 负责什么

- 项目注册与路由；
- 项目级状态；
- 最大目标与边界；
- 跨项目依赖；
- 项目级关键决策与坑点；
- 执行仓库、入口、资产来源的指针。

## AIPM 不负责什么

不复制执行仓库已经维护的：
- 具体产品事实；
- Drive Folder/File ID 明细；
- 某张图的版本；
- 某 SKU 关键词全文；
- 项目内部详细规则。

这些应留在对应执行仓库或实际资产源。

## 项目启动

新增长期复杂项目时至少明确：
- `PROJECT_SCOPE`：最大目标与边界；
- `CURRENT_STATE`：项目级当前状态与执行入口；
- `DECISION_LOG`：重大决策；
- `PITFALLS`：已知坑点；
- `ASSET_INDEX`：只记录权威资产/执行入口，不复制大文件。

复杂项目按需要增加 `TASK_QUEUE`，不是强制模板。

## 存储分工

### AIPM
项目路由、项目级状态、边界、重大决策、跨项目依赖。

### 执行仓库
该项目 ACTIVE 规则、详细状态、事实、索引、Manifest、执行逻辑。

### Google Drive / API / 外部服务
实际图片、视频、表格、PDF、大型原始资料和实时数据。

## 冲突处理

如果 AIPM 摘要与执行仓库最新 ACTIVE 状态冲突：
- 以执行仓库为准；
- 只有冲突影响项目状态、入口、边界或跨项目关系时，才回写 AIPM。

核心原则：**先路由，后执行；明确任务走 Fast Path，模糊任务走 Discovery Path；只读取最小必要上下文。**
