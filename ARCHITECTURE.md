# AIPM 架构｜Control Plane / Execution Plane

状态：ACTIVE

## 1. 核心定位

AIPM 不再承担各项目内部执行细节，而作为所有长期 AI 项目的 **控制面（Control Plane）**：

- 识别当前任务属于哪个项目；
- 告诉新窗口应该进入哪个执行仓库/入口；
- 保存项目级状态、边界、关键决策和跨项目依赖；
- 防止同一事实在多个仓库重复维护。

具体项目仓库是 **执行面（Execution Plane）**：
- 保存该项目 ACTIVE 规则；
- 保存项目内部事实、状态、索引、Manifest、执行流程；
- 直接完成项目任务。

Google Drive / 外部服务是 **资产与数据面（Data Plane）**：
- 保存图片、视频、表格、PDF、大型原始资料及其他实际数据。

```text
用户任务
   ↓
AIPM Control Plane（仅在需要路由时）
   ↓
Execution Repo / AGENTS.md
   ↓
最小必要项目上下文
   ↓
Drive / API / 实际资产
```

## 2. 两种启动路径

### Fast Path｜项目已明确

当用户已经明确说出具体产品、项目或明显属于某个执行仓库的任务时：

1. 直接进入该项目 `AGENTS.md` 或等价硬入口；
2. 按入口规则读取最小必要上下文；
3. 不为了“走流程”强制读取完整 AIPM。

例如：
- “继续叉车富内容” → 直接进入 `ozon-WB/AGENTS.md`；
- “改 VF520 参数表” → 直接进入 VF520 项目入口。

### Discovery Path｜项目不明确 / 跨项目

以下情况先读 AIPM：
- 用户只说“继续之前那个”；
- 任务可能属于多个项目；
- 涉及跨项目优先级、资源、计划或依赖；
- 需要判断应该去哪个仓库/Drive/工具；
- 项目入口未知或怀疑已变化。

读取顺序：
1. `AI_START_HERE.md`
2. `PROJECT_INDEX.md`
3. 对应 `PROJECTS/<PROJECT_ID>/CURRENT_STATE.md`
4. 再跳转到执行仓库入口。

## 3. 单一事实源原则

AIPM 只保存项目级摘要和指针，不复制执行仓库细节。

- AIPM：项目身份、状态、入口、边界、跨项目依赖、重大决策。
- 执行仓库：项目内部 ACTIVE 规则、详细状态、产品事实、索引、Manifest。
- Drive/API：实际文件和数据。

如果 AIPM 摘要与执行仓库冲突：
1. 以执行仓库最新 ACTIVE 状态为准；
2. 若冲突影响路由/项目状态，再回写 AIPM；
3. 不在两个地方长期维护同一份详细事实。

## 4. 项目入口契约

每个长期执行项目应尽量提供一个稳定硬入口：

- 首选：仓库根目录 `AGENTS.md`
- 次选：明确的 `START_HERE.md` / Skill 入口

AIPM 的 `PROJECT_INDEX.md` 必须记录：
- Project ID
- 触发关键词/别名
- 项目状态
- 执行仓库
- 硬入口
- 项目级状态来源
- 资产/数据来源

新窗口通过这个契约完成路由，不依赖聊天记忆。

## 5. 状态同步原则

不是所有执行变化都回写 AIPM。

### 只写执行仓库
- 某张商品图替换；
- 某关键词调整；
- 某产品卖点确认；
- 某 Drive File ID 变化；
- 项目内部普通规则变化。

### 同时更新 AIPM
- 项目暂停 / 恢复 / 完成；
- 项目执行仓库或硬入口变化；
- 项目最大目标变化；
- 跨项目依赖变化；
- 原有路由逻辑失效；
- 项目级重大架构发生变化。

这样避免“双重记账”。

## 6. 架构变更治理

重大架构变更必须满足：
- 有明确问题要解决；
- 指定唯一 owner / source of truth；
- 优先替换旧结构，而不是新增平行结构；
- 能减少重复读取、重复维护或冲突；
- 记录在对应 `DECISION_LOG`，必要时标记旧决策为 superseded。

核心目标：**AIPM 负责找路，执行仓库负责做事，Drive/API 负责存真实资产；明确任务走 Fast Path，模糊任务才走 Discovery Path。**
