# AI 多角色协作规则

## 角色定义

主控 AI：负责目标、规划、状态维护和决策记录。

执行 AI / Codex：负责具体任务执行，结果必须回写对应执行仓库或数据源。

审核 AI：负责独立检查风险、逻辑和是否符合目标。

## 原则

所有角色共享 GitHub 当前状态，不依赖单个聊天窗口记忆。

重要信息必须记录：
- 当前状态
- 决策原因
- 已知风险
- 下一步任务

AIPM 负责项目路由、总状态和跨项目治理；具体执行细节留在对应执行仓库，避免双重维护。

## 多窗口 / 多 Agent 并发规则

多个窗口可以同时工作，但并发不等于各自维护一套状态。

硬规则：**写前刷新，写后确认，冲突不覆盖。**

### 写前刷新
1. 修改 GitHub 文件前重新读取目标文件最新版本和最新 SHA。
2. 不使用聊天早期保存的旧全文覆盖当前文件。
3. 若发现其他窗口已经提交新内容，先理解和保留最新变化，再叠加本任务的最小修改。
4. 修改外部资产前，如存在并发可能，先确认当前对象仍是预期版本。

### 写后确认
1. 确认 GitHub 提交/外部数据修改真实成功。
2. 确认关键入口、状态和映射仍指向当前有效对象。
3. 需要长期复用的变化完成语义/状态回写后，任务才算完成。

### 冲突处理
1. GitHub 出现 SHA 冲突时，不强制覆盖；重新读取最新文件并重新合并。
2. 同一事实出现两个并发版本时，不擅自用时间戳决定业务真相；优先依据用户最新明确意图和权威数据源。
3. 无法确定最新意图时，保留现状并标记待确认，避免破坏其他窗口成果。
4. 尽量让不同窗口处理不同项目、产品、文件或职责，减少同一文件成为写入热点。

## 回写边界

执行仓库内部的小变化不需要回 AIPM。

仅当以下内容变化时更新 AIPM：
- 项目状态（进行中 / 暂停 / 完成）；
- 项目入口或执行仓库；
- PROJECT_SCOPE / 项目边界；
- 跨项目依赖；
- 会改变未来路由的大架构决策。

原则：**AIPM 维护控制面，执行仓库维护执行面；共享最新状态，但不重复保存执行细节。**
